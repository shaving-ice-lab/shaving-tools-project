# 性能衰退追踪器 - 开发文档

> **项目编号**: 11  
> **项目目录**: `apps/degradation-tracker`  
> **优先级**: P1（高优先级）  
> **状态**: 🟢 已完成  
> **预计工期**: 2-3周  
> **技术难度**: ⭐⭐⭐⭐☆

---

## 📋 项目概述

性能衰退追踪器是一套包含 **Web App (PWA)** 和 **桌面端展示程序** 的完整解决方案。Web App负责定期采集设备性能数据，分析性能衰退趋势，预测设备寿命；桌面端负责接收、存储和可视化展示长期追踪数据。

### 系统架构

```text
┌─────────────────┐      无线/有线传输      ┌─────────────────┐
│   Web App       │ ◄──────────────────────► │    桌面端程序    │
│  (数据采集端)    │    WebSocket / USB      │   (数据展示端)   │
└─────────────────┘                          └─────────────────┘
```

### 核心价值

1. **长期性能追踪**: 记录设备性能随时间的变化趋势
2. **智能预测**: 基于历史数据预测设备剩余寿命
3. **横向对比**: 与同型号、同龄设备进行性能对比
4. **健康报告**: 生成专业的设备健康评估报告
5. **桌面端大屏展示**: 长期数据可视化与深度分析

---

## 🛠️ 技术栈

### Web App

| 类别         | 技术选型              | 说明             |
| ------------ | --------------------- | ---------------- |
| **框架**     | Next.js 14            | React全栈框架    |
| **语言**     | TypeScript            | 类型安全         |
| **样式**     | TailwindCSS           | 原子化CSS        |
| **UI组件**   | shadcn/ui             | 现代组件库       |
| **图表**     | Chart.js / Recharts   | 数据可视化       |
| **PWA**      | next-pwa              | 离线支持         |
| **存储**     | IndexedDB (Dexie.js)  | 本地数据库       |
| **部署**     | Vercel                | 边缘部署         |

### 桌面端程序

| 类别         | 技术选型              | 说明             |
| ------------ | --------------------- | ---------------- |
| **框架**     | Electron + React      | 跨平台桌面应用   |
| **UI库**     | TailwindCSS + shadcn  | 现代UI组件       |
| **图表**     | Chart.js / Recharts   | 数据可视化       |
| **通信**     | WebSocket             | 实时数据传输     |
| **数据存储** | SQLite                | 本地数据持久化   |
| **机器学习** | TensorFlow.js         | 寿命预测模型     |

---

## 🎯 功能模块

### Web App 功能

#### 1. 性能测试套件

- **CPU性能测试**: 单核/多核计算性能
- **GPU性能测试**: 图形渲染性能
- **内存性能测试**: 读写速度、容量
- **存储性能测试**: 读写速度、可用空间
- **电池健康测试**: 充放电效率、容量衰减

#### 2. 定期测试调度

- **自动测试**: 定时自动执行性能测试
- **测试提醒**: 通知用户进行测试
- **测试频率设置**: 每日/每周/每月
- **后台测试**: PWA后台静默测试

#### 3. 衰退分析

- **性能趋势图**: 各项指标随时间变化曲线
- **衰退速率计算**: 每月/每年性能下降百分比
- **异常检测**: 识别性能突然下降
- **分项评分**: 各性能维度独立评分

#### 4. 寿命预测

- **剩余寿命预估**: 基于衰退曲线预测
- **置换建议**: 建议更换时间点
- **风险预警**: 关键指标预警提示

#### 5. 设备对比

- **同型号对比**: 与相同型号设备对比
- **同龄对比**: 与相同使用时长设备对比
- **排名展示**: 性能在同类设备中的位置

#### 6. 健康报告

- **综合健康评分**: 0-100分
- **各项指标详情**: 分项评分与说明
- **建议与优化**: 延长寿命的建议
- **报告分享**: 导出/分享报告

#### 7. 数据同步

- **实时同步**: WebSocket推送到桌面端
- **批量同步**: 一次性同步历史数据
- **增量同步**: 仅同步新增数据

### 桌面端功能

#### 1. 设备连接

- 自动发现局域网设备
- USB连接检测
- 多设备管理
- 连接状态监控

#### 2. 数据展示

- 长期性能趋势图
- 多设备对比视图
- 衰退速率仪表盘
- 寿命预测时间轴

#### 3. 深度分析

- 多维度数据分析
- 自定义时间范围
- 数据过滤筛选
- 统计报表生成

#### 4. 设备档案

- 设备信息管理
- 测试历史记录
- 报告存档查看

#### 5. 报告导出

- PDF健康报告
- Excel数据导出
- 图表图片导出

---

## 📁 项目目录结构

### Web App

```text
apps/degradation-tracker/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── test/
│   │   ├── page.tsx
│   │   ├── cpu/page.tsx
│   │   ├── gpu/page.tsx
│   │   ├── memory/page.tsx
│   │   └── storage/page.tsx
│   ├── analysis/
│   │   └── page.tsx
│   ├── prediction/
│   │   └── page.tsx
│   ├── compare/
│   │   └── page.tsx
│   ├── report/
│   │   └── page.tsx
│   ├── history/
│   │   └── page.tsx
│   └── settings/
│       └── page.tsx
│
├── components/
│   ├── ui/
│   ├── test/
│   │   ├── CpuTest.tsx
│   │   ├── GpuTest.tsx
│   │   ├── MemoryTest.tsx
│   │   └── TestProgress.tsx
│   ├── analysis/
│   │   ├── TrendChart.tsx
│   │   ├── DegradationRate.tsx
│   │   └── ScoreCard.tsx
│   ├── prediction/
│   │   ├── LifespanPredictor.tsx
│   │   └── RiskIndicator.tsx
│   ├── compare/
│   │   ├── DeviceCompare.tsx
│   │   └── RankingList.tsx
│   └── sync/
│       └── SyncPanel.tsx
│
├── lib/
│   ├── tests/
│   │   ├── cpu-test.ts
│   │   ├── gpu-test.ts
│   │   ├── memory-test.ts
│   │   └── storage-test.ts
│   ├── analysis/
│   │   ├── degradation.ts
│   │   └── prediction.ts
│   ├── database.ts
│   └── websocket.ts
│
├── hooks/
│   ├── usePerformanceTest.ts
│   ├── useDegradation.ts
│   └── useSync.ts
│
├── public/
│   └── manifest.json
│
├── next.config.js
├── package.json
└── tsconfig.json
```

### 桌面端

```text
apps/degradation-tracker/
├── desktop/
│   ├── src/
│   │   ├── main/
│   │   │   ├── index.ts
│   │   │   ├── websocket.ts
│   │   │   └── database.ts
│   │   │
│   │   ├── renderer/
│   │   │   ├── App.tsx
│   │   │   ├── components/
│   │   │   │   ├── DevicePanel.tsx
│   │   │   │   ├── TrendChart.tsx
│   │   │   │   ├── PredictionView.tsx
│   │   │   │   ├── CompareView.tsx
│   │   │   │   └── DeviceArchive.tsx
│   │   │   ├── pages/
│   │   │   │   ├── Dashboard.tsx
│   │   │   │   ├── Analysis.tsx
│   │   │   │   ├── Devices.tsx
│   │   │   │   ├── Reports.tsx
│   │   │   │   └── Settings.tsx
│   │   │   └── hooks/
│   │   │
│   │   └── preload/
│   │       └── index.ts
│   │
│   ├── package.json
│   └── electron-builder.json
```

---

## 📡 数据传输协议

### WebSocket协议（无线传输）

```typescript
// 连接: ws://desktop_ip:8767

interface DegradationPacket {
  type: 'test_result' | 'sync_history' | 'device_info';
  timestamp: number;
  deviceId: string;
  data: TestResult | HistoryData[] | DeviceInfo;
}

interface TestResult {
  testId: string;
  testDate: string;
  cpuScore: number;
  gpuScore: number;
  memoryScore: number;
  storageScore: number;
  batteryHealth: number;
  overallScore: number;
  details: {
    cpuSingleCore: number;
    cpuMultiCore: number;
    gpuFps: number;
    memorySpeed: number;
    storageRead: number;
    storageWrite: number;
  };
}

interface DeviceInfo {
  model: string;
  manufacturer: string;
  os: string;
  osVersion: string;
  purchaseDate?: string;
  totalTests: number;
  firstTestDate: string;
}

interface DegradationAnalysis {
  cpuDegradation: number;     // 每年衰退百分比
  gpuDegradation: number;
  memoryDegradation: number;
  storageDegradation: number;
  overallDegradation: number;
  predictedLifespan: number;  // 预计剩余月数
  healthGrade: 'A' | 'B' | 'C' | 'D' | 'F';
}
```

### USB传输

```text
通过本地HTTP服务器实现:
1. 桌面端启动本地服务器 localhost:8767
2. 手机通过USB网络共享连接电脑
3. Web App 连接 localhost:8767 进行数据传输
```

---

## 📋 开发任务清单 (TODO List)

### Web App

#### 项目初始化

- [x] 创建Next.js项目
- [x] 配置TypeScript
- [x] 配置TailwindCSS + shadcn/ui
- [x] 配置PWA (next-pwa)
- [x] 配置IndexedDB (Dexie.js)
- [x] 配置Chart.js / Recharts

#### 性能测试模块

- [x] 实现CPU性能测试（单核）
- [x] 实现CPU性能测试（多核）
- [x] 实现GPU性能测试（WebGL）
- [x] 实现内存性能测试
- [x] 实现存储性能测试
- [x] 实现电池健康检测
- [x] 设计测试进度UI
- [x] 设计测试结果展示UI

#### 定期测试调度

- [x] 实现测试调度器
- [x] 实现测试提醒通知
- [x] 实现PWA后台任务
- [x] 实现测试频率设置

#### 衰退分析模块

- [x] 实现性能趋势计算
- [x] 实现衰退速率算法
- [x] 实现异常检测算法
- [x] 实现分项评分算法
- [x] 设计趋势图表组件
- [x] 设计衰退率展示UI

#### 寿命预测模块

- [x] 实现线性回归预测
- [x] 实现剩余寿命计算
- [x] 实现风险评估算法
- [x] 设计预测结果UI
- [x] 设计预警提示组件

#### 设备对比模块

- [x] 实现设备数据匿名上报（可选）
- [x] 实现同型号设备对比
- [x] 实现同龄设备对比
- [x] 实现排名计算
- [x] 设计对比图表UI

#### 健康报告模块

- [x] 实现综合评分算法
- [x] 实现报告数据模型
- [x] 实现报告生成逻辑
- [x] 设计报告展示页面
- [x] 实现报告分享功能

#### 数据存储

- [x] 设计IndexedDB Schema
- [x] 实现测试记录CRUD
- [x] 实现设备信息存储
- [x] 实现数据导出功能

#### 数据同步

- [x] 实现WebSocket客户端
- [x] 实现自动发现桌面端
- [x] 实现实时数据推送
- [x] 实现批量历史同步
- [x] 实现增量同步

#### UI页面

- [x] 首页（健康概览）
- [x] 性能测试页
- [x] 衰退分析页
- [x] 寿命预测页
- [x] 设备对比页
- [x] 健康报告页
- [x] 历史记录页
- [x] 设置页

### 桌面端程序

#### 项目初始化

- [x] 创建Electron + React项目
- [x] 配置TypeScript
- [x] 配置TailwindCSS + shadcn/ui
- [x] 配置Chart.js
- [x] 配置electron-builder

#### 通信模块

- [x] 实现WebSocket服务器
- [x] 实现UDP广播发现
- [x] 实现USB连接检测
- [x] 实现多设备管理

#### 数据存储

- [x] 实现SQLite数据库
- [x] 设计数据表结构
- [x] 实现数据CRUD
- [x] 实现数据导出

#### UI开发

- [x] 设备连接面板
- [x] 仪表盘（多设备概览）
- [x] 长期趋势图表
- [x] 衰退速率展示
- [x] 寿命预测时间轴
- [x] 多设备对比视图
- [x] 设备档案页
- [x] 测试历史列表
- [x] 报告存档页
- [x] 设置页面

#### 分析功能

- [x] 多维度数据分析
- [x] 自定义时间范围筛选
- [x] 统计报表生成

#### 报告导出

- [x] PDF健康报告生成
- [x] Excel数据导出
- [x] 图表图片导出

### 测试与发布

#### 测试

- [ ] Web App功能测试
- [ ] 桌面端功能测试
- [ ] 传输稳定性测试
- [ ] PWA离线测试
- [ ] 预测算法准确性测试

#### 发布

- [ ] Vercel部署Web App
- [ ] 桌面端Windows打包
- [ ] 桌面端macOS打包
- [ ] 编写使用文档

---

## 🔑 核心技术实现

### CPU性能测试

```typescript
class CpuBenchmark {
  // 单核测试 - 质数计算
  async singleCoreTest(): Promise<number> {
    const start = performance.now();
    let count = 0;
    
    for (let i = 2; i < 100000; i++) {
      if (this.isPrime(i)) count++;
    }
    
    const duration = performance.now() - start;
    // 归一化分数 (越快分数越高)
    return Math.round(10000 / duration * 1000);
  }
  
  // 多核测试 - Web Workers
  async multiCoreTest(): Promise<number> {
    const cores = navigator.hardwareConcurrency || 4;
    const workers: Worker[] = [];
    const results: number[] = [];
    
    const start = performance.now();
    
    await Promise.all(
      Array(cores).fill(0).map((_, i) => 
        new Promise<void>((resolve) => {
          const worker = new Worker('/workers/cpu-worker.js');
          worker.onmessage = (e) => {
            results.push(e.data);
            resolve();
          };
          worker.postMessage({ workload: 25000 });
          workers.push(worker);
        })
      )
    );
    
    const duration = performance.now() - start;
    workers.forEach(w => w.terminate());
    
    return Math.round(10000 / duration * 1000 * cores);
  }
  
  private isPrime(n: number): boolean {
    if (n < 2) return false;
    for (let i = 2; i <= Math.sqrt(n); i++) {
      if (n % i === 0) return false;
    }
    return true;
  }
}
```

### GPU性能测试

```typescript
class GpuBenchmark {
  private canvas: HTMLCanvasElement;
  private gl: WebGLRenderingContext;
  
  async runTest(): Promise<number> {
    const frameCount = 300;
    const frames: number[] = [];
    let lastTime = performance.now();
    
    for (let i = 0; i < frameCount; i++) {
      this.renderScene(i);
      
      const now = performance.now();
      frames.push(now - lastTime);
      lastTime = now;
      
      await new Promise(r => requestAnimationFrame(r));
    }
    
    const avgFrameTime = frames.reduce((a, b) => a + b) / frames.length;
    const fps = 1000 / avgFrameTime;
    
    // 归一化分数
    return Math.round(fps * 100);
  }
  
  private renderScene(frame: number) {
    // WebGL渲染复杂场景
    // 包含大量三角形、纹理、光照计算
  }
}
```

### 衰退分析算法

```typescript
class DegradationAnalyzer {
  // 计算衰退速率 (每年百分比)
  calculateDegradationRate(records: TestRecord[]): number {
    if (records.length < 2) return 0;
    
    // 按时间排序
    const sorted = [...records].sort((a, b) => 
      new Date(a.testDate).getTime() - new Date(b.testDate).getTime()
    );
    
    const firstScore = sorted[0].overallScore;
    const lastScore = sorted[sorted.length - 1].overallScore;
    
    const firstDate = new Date(sorted[0].testDate);
    const lastDate = new Date(sorted[sorted.length - 1].testDate);
    const yearsDiff = (lastDate.getTime() - firstDate.getTime()) / 
                      (365 * 24 * 60 * 60 * 1000);
    
    if (yearsDiff < 0.1) return 0;
    
    const totalDegradation = ((firstScore - lastScore) / firstScore) * 100;
    return totalDegradation / yearsDiff;
  }
  
  // 预测剩余寿命 (月)
  predictLifespan(
    currentScore: number,
    degradationRate: number,
    threshold: number = 50
  ): number {
    if (degradationRate <= 0) return 120; // 10年
    
    const percentToThreshold = ((currentScore - threshold) / currentScore) * 100;
    const yearsRemaining = percentToThreshold / degradationRate;
    
    return Math.max(0, Math.round(yearsRemaining * 12));
  }
  
  // 健康等级评定
  getHealthGrade(score: number, ageMonths: number): string {
    // 根据设备年龄调整期望值
    const expectedScore = 100 - (ageMonths * 0.5);
    const ratio = score / expectedScore;
    
    if (ratio >= 0.95) return 'A';
    if (ratio >= 0.85) return 'B';
    if (ratio >= 0.70) return 'C';
    if (ratio >= 0.50) return 'D';
    return 'F';
  }
}
```

### 数据同步

```typescript
class SyncClient {
  private ws: WebSocket | null = null;
  
  async connect(serverUrl: string) {
    this.ws = new WebSocket(serverUrl);
    
    this.ws.onclose = () => {
      setTimeout(() => this.connect(serverUrl), 3000);
    };
  }
  
  // 同步测试结果
  sendTestResult(result: TestResult) {
    this.send({
      type: 'test_result',
      timestamp: Date.now(),
      deviceId: this.getDeviceId(),
      data: result
    });
  }
  
  // 批量同步历史
  async syncHistory(records: TestResult[]) {
    this.send({
      type: 'sync_history',
      timestamp: Date.now(),
      deviceId: this.getDeviceId(),
      data: records
    });
  }
  
  private send(packet: DegradationPacket) {
    if (this.ws?.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify(packet));
    }
  }
}
```

---

## ⚠️ 技术风险与解决方案

### 风险1: 性能测试一致性

**问题**: 不同测试环境（温度、后台应用）影响测试结果

**解决方案**:
1. 测试前检测设备状态
2. 多次测试取中位数
3. 标记异常测试结果
4. 建议测试条件（充电、静置后）

### 风险2: 预测准确性

**问题**: 线性预测可能不符合实际衰退曲线

**解决方案**:
1. 采用多种预测模型对比
2. 根据数据量调整置信度
3. 标明预测仅供参考
4. 随数据积累优化模型

### 风险3: 设备对比数据

**问题**: 需要其他用户数据进行对比

**解决方案**:
1. 匿名上报机制（用户可选）
2. 预设同型号基准数据
3. 仅同账户多设备对比

### 风险4: PWA后台限制

**问题**: 浏览器可能限制后台任务

**解决方案**:
1. 使用Web Push提醒用户打开
2. 提供手动测试入口
3. 记录上次测试时间提醒

---

## 📊 验收标准

### 功能验收

- [ ] 性能测试结果稳定可复现
- [ ] 衰退分析图表正确显示
- [ ] 寿命预测结果合理
- [ ] 健康报告内容完整
- [ ] 数据同步延迟<500ms
- [ ] PWA离线正常工作

### 性能验收

- [ ] 首屏加载<3s
- [ ] 测试过程流畅
- [ ] 图表渲染流畅
- [ ] 桌面端内存<300MB

### 兼容性验收

- [ ] Chrome/Edge/Firefox/Safari
- [ ] iOS Safari / Android Chrome
- [ ] Windows 10/11
- [ ] macOS 10.15+

---

## 📚 参考资料

1. [Web Performance API](https://developer.mozilla.org/en-US/docs/Web/API/Performance_API)
2. [Web Workers](https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API)
3. [WebGL](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API)
4. [Chart.js](https://www.chartjs.org/)
5. [Dexie.js](https://dexie.org/)
6. [TensorFlow.js](https://www.tensorflow.org/js)

---

## 📝 更新日志

| 日期       | 版本 | 更新内容       |
| ---------- | ---- | -------------- |
| 2025-12-27 | v0.1 | 初始文档创建   |
| 2025-01-XX | v1.0 | Web App和桌面端开发完成 |

---

**文档维护者**: 开发团队  
**最后更新**: 2025年12月27日
