# 屏幕显示测试工具 - 详细开发文档

> **项目编号**: 02  
> **项目目录**: `apps/web/app/screen-test`  
> **优先级**: P0（最高优先级）  
> **状态**: 🟡 待开发  
> **预计工期**: 2周  
> **技术难度**: ⭐⭐⭐☆☆

---

## 📋 项目概述

屏幕显示测试工具是一款 **Web + PWA 应用**，用于专业级屏幕质量检测与分析。应用支持坏点检测、色彩准确度测试、刷新率检测、PWM频闪检测等多种测试模式，帮助用户全面评估手机/显示器屏幕的显示质量。

### 为什么选择Web方案？

| 原生方案局限性 | Web方案优势 |
|--------------|-------------|
| 需要安装，用户门槛高 | 即开即用，无需安装 |
| 跨平台需多套代码 | 一套代码适配所有设备 |
| 更新需重新下载 | 实时更新，用户无感知 |
| 无法测试电脑显示器 | 支持任何带浏览器的设备 |

### 项目目标

1. **坏点检测**: 通过纯色背景快速识别屏幕坏点/亮点
2. **色彩测试**: 评估屏幕色彩准确度、色域覆盖
3. **刷新率测试**: 实时检测并显示屏幕刷新率
4. **PWM检测**: 检测屏幕是否存在低频PWM频闪
5. **响应时间**: 通过拖影测试评估屏幕响应速度
6. **均匀性测试**: 检测屏幕亮度是否均匀
7. **HDR测试**: 评估HDR显示效果
8. **OLED烧屏风险**: 提供烧屏风险检测图案

---

## 🛠️ 技术栈

| 类别 | 技术选型 | 说明 |
|------|----------|------|
| **框架** | Next.js 14 (App Router) | React全栈框架 |
| **语言** | TypeScript | 类型安全 |
| **UI框架** | TailwindCSS | 原子化CSS |
| **组件库** | shadcn/ui | 高质量组件 |
| **图标** | Lucide React | 现代图标库 |
| **图形渲染** | Canvas API / WebGL | 高精度绘图 |
| **动画** | requestAnimationFrame | 精准帧控制 |
| **传感器** | DeviceOrientation API | 陀螺仪数据 |
| **全屏** | Fullscreen API | 全屏测试模式 |
| **PWA** | next-pwa | 离线支持 |

---

## 🎯 核心功能模块

### 模块1: 坏点检测

**功能描述**: 通过纯色全屏背景，帮助用户肉眼识别屏幕坏点、亮点、暗点

**功能点**:
- 纯色背景切换（红/绿/蓝/白/黑/灰）
- 全屏模式显示
- 手势/按键切换颜色
- 坏点标记功能（可选）
- 检测指引说明

**技术实现**:

```typescript
// 坏点检测组件
interface DeadPixelTestProps {
  onComplete?: () => void;
}

const COLORS = [
  { name: '纯白', value: '#FFFFFF', description: '检测黑点/暗点' },
  { name: '纯黑', value: '#000000', description: '检测亮点/白点' },
  { name: '纯红', value: '#FF0000', description: '检测红色子像素' },
  { name: '纯绿', value: '#00FF00', description: '检测绿色子像素' },
  { name: '纯蓝', value: '#0000FF', description: '检测蓝色子像素' },
  { name: '灰色50%', value: '#808080', description: '检测亮度不均' },
];

export function DeadPixelTest({ onComplete }: DeadPixelTestProps) {
  const [colorIndex, setColorIndex] = useState(0);
  const [isFullscreen, setIsFullscreen] = useState(false);
  const containerRef = useRef<HTMLDivElement>(null);

  const enterFullscreen = async () => {
    if (containerRef.current) {
      await containerRef.current.requestFullscreen();
      setIsFullscreen(true);
    }
  };

  const nextColor = () => {
    if (colorIndex < COLORS.length - 1) {
      setColorIndex(prev => prev + 1);
    } else {
      onComplete?.();
    }
  };

  // 键盘和触摸事件监听
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.key === ' ' || e.key === 'ArrowRight') nextColor();
      if (e.key === 'ArrowLeft') setColorIndex(prev => Math.max(0, prev - 1));
      if (e.key === 'Escape') document.exitFullscreen();
    };
    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, [colorIndex]);

  return (
    <div
      ref={containerRef}
      className="w-full h-screen cursor-pointer transition-colors duration-200"
      style={{ backgroundColor: COLORS[colorIndex].value }}
      onClick={nextColor}
    >
      {!isFullscreen && (
        <div className="absolute inset-0 flex items-center justify-center">
          <Button onClick={enterFullscreen}>开始全屏检测</Button>
        </div>
      )}
    </div>
  );
}
```

---

### 模块2: 刷新率测试 ⭐核心

**功能描述**: 使用requestAnimationFrame精准测量屏幕实际刷新率

**功能点**:
- 实时显示当前刷新率（Hz）
- 帧时间统计（最小/最大/平均）
- 刷新率波动图表
- 支持检测高刷屏（90/120/144/165Hz）
- 丢帧检测与提示

**技术实现**:

```typescript
interface FrameData {
  timestamp: number;
  delta: number;
}

export function RefreshRateTest() {
  const [fps, setFps] = useState(0);
  const [avgFps, setAvgFps] = useState(0);
  const [minDelta, setMinDelta] = useState(Infinity);
  const [maxDelta, setMaxDelta] = useState(0);
  const frameDataRef = useRef<FrameData[]>([]);
  const lastTimeRef = useRef(0);
  const frameCountRef = useRef(0);
  const animationRef = useRef<number>();

  useEffect(() => {
    let isRunning = true;
    const sampleWindow = 60; // 采样窗口

    const measureFrame = (timestamp: number) => {
      if (!isRunning) return;

      if (lastTimeRef.current > 0) {
        const delta = timestamp - lastTimeRef.current;
        frameDataRef.current.push({ timestamp, delta });

        // 保持固定窗口大小
        if (frameDataRef.current.length > sampleWindow) {
          frameDataRef.current.shift();
        }

        // 计算统计数据
        const deltas = frameDataRef.current.map(f => f.delta);
        const avgDelta = deltas.reduce((a, b) => a + b, 0) / deltas.length;
        const currentFps = Math.round(1000 / delta);
        const calculatedAvgFps = Math.round(1000 / avgDelta);

        setFps(currentFps);
        setAvgFps(calculatedAvgFps);
        setMinDelta(Math.min(...deltas));
        setMaxDelta(Math.max(...deltas));
      }

      lastTimeRef.current = timestamp;
      frameCountRef.current++;
      animationRef.current = requestAnimationFrame(measureFrame);
    };

    animationRef.current = requestAnimationFrame(measureFrame);

    return () => {
      isRunning = false;
      if (animationRef.current) {
        cancelAnimationFrame(animationRef.current);
      }
    };
  }, []);

  const getRefreshRateLabel = (fps: number) => {
    if (fps >= 140) return '144Hz+';
    if (fps >= 115) return '120Hz';
    if (fps >= 85) return '90Hz';
    if (fps >= 55) return '60Hz';
    return '低刷新率';
  };

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>屏幕刷新率测试</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="text-center">
            <div className="text-6xl font-bold text-primary">{avgFps} Hz</div>
            <div className="text-muted-foreground mt-2">
              {getRefreshRateLabel(avgFps)}
            </div>
          </div>
          
          <div className="grid grid-cols-3 gap-4 mt-6">
            <div className="text-center">
              <div className="text-2xl font-semibold">{fps}</div>
              <div className="text-sm text-muted-foreground">当前FPS</div>
            </div>
            <div className="text-center">
              <div className="text-2xl font-semibold">{minDelta.toFixed(2)}ms</div>
              <div className="text-sm text-muted-foreground">最小帧时间</div>
            </div>
            <div className="text-center">
              <div className="text-2xl font-semibold">{maxDelta.toFixed(2)}ms</div>
              <div className="text-sm text-muted-foreground">最大帧时间</div>
            </div>
          </div>
        </CardContent>
      </Card>

      {/* 动态方块指示器 - 帮助用户直观感受刷新率 */}
      <RefreshRateVisualizer />
    </div>
  );
}

// 刷新率可视化组件 - 移动方块
function RefreshRateVisualizer() {
  const canvasRef = useRef<HTMLCanvasElement>(null);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;

    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    let x = 0;
    const speed = 5; // 像素/帧
    const boxSize = 30;

    const animate = () => {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      
      // 绘制移动方块
      ctx.fillStyle = '#22c55e';
      ctx.fillRect(x, (canvas.height - boxSize) / 2, boxSize, boxSize);
      
      x += speed;
      if (x > canvas.width) x = -boxSize;

      requestAnimationFrame(animate);
    };

    animate();
  }, []);

  return (
    <canvas
      ref={canvasRef}
      width={800}
      height={100}
      className="w-full h-24 bg-black rounded-lg"
    />
  );
}
```

---

### 模块3: PWM频闪检测

**功能描述**: 通过高速闪烁图案模拟相机快门效果，帮助用户检测屏幕PWM频闪

**功能点**:
- 多频率闪烁图案生成
- 模拟不同PWM频率效果
- 检测指导说明
- 相机拍摄建议

**技术实现**:

```typescript
interface PWMTestConfig {
  frequency: number;  // Hz
  name: string;
  description: string;
  risk: 'low' | 'medium' | 'high';
}

const PWM_CONFIGS: PWMTestConfig[] = [
  { frequency: 60, name: '60Hz', description: '低频PWM，对眼睛伤害较大', risk: 'high' },
  { frequency: 120, name: '120Hz', description: '中低频PWM', risk: 'high' },
  { frequency: 240, name: '240Hz', description: '中频PWM', risk: 'medium' },
  { frequency: 480, name: '480Hz', description: '中高频PWM', risk: 'medium' },
  { frequency: 960, name: '960Hz', description: '高频PWM，相对护眼', risk: 'low' },
  { frequency: 1920, name: '1920Hz+', description: '超高频/DC调光，护眼', risk: 'low' },
];

export function PWMFlickerTest() {
  const [isRunning, setIsRunning] = useState(false);
  const [selectedFreq, setSelectedFreq] = useState(240);
  const canvasRef = useRef<HTMLCanvasElement>(null);

  useEffect(() => {
    if (!isRunning || !canvasRef.current) return;

    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    let isWhite = true;
    const intervalMs = 1000 / (selectedFreq * 2); // 半周期

    const interval = setInterval(() => {
      ctx.fillStyle = isWhite ? '#FFFFFF' : '#000000';
      ctx.fillRect(0, 0, canvas.width, canvas.height);
      isWhite = !isWhite;
    }, intervalMs);

    return () => clearInterval(interval);
  }, [isRunning, selectedFreq]);

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>PWM频闪检测</CardTitle>
          <CardDescription>
            用手机相机对准屏幕拍摄，如果看到条纹则说明存在PWM频闪
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-4">
          <div className="flex flex-wrap gap-2">
            {PWM_CONFIGS.map(config => (
              <Button
                key={config.frequency}
                variant={selectedFreq === config.frequency ? 'default' : 'outline'}
                onClick={() => setSelectedFreq(config.frequency)}
              >
                {config.name}
              </Button>
            ))}
          </div>

          <canvas
            ref={canvasRef}
            width={400}
            height={300}
            className="w-full h-64 border rounded-lg"
          />

          <Button
            className="w-full"
            onClick={() => setIsRunning(!isRunning)}
          >
            {isRunning ? '停止测试' : '开始测试'}
          </Button>

          <Alert>
            <AlertDescription>
              💡 提示：将手机相机快门速度调至1/1000s或更快，对准测试区域拍摄。
              如果照片中出现明显的黑色条纹，说明屏幕使用了低频PWM调光。
            </AlertDescription>
          </Alert>
        </CardContent>
      </Card>
    </div>
  );
}
```

---

### 模块4: 色彩准确度测试

**功能描述**: 显示标准色卡，用户对比评估屏幕色彩还原能力

**功能点**:
- sRGB标准色卡显示
- 24色ColorChecker色卡
- 灰阶测试（0-255级）
- 色彩渐变测试
- 原色/补色测试

**技术实现**:

```typescript
// 标准24色ColorChecker色卡
const COLOR_CHECKER_COLORS = [
  // 第一行 - 自然色
  { name: 'Dark Skin', srgb: '#735244' },
  { name: 'Light Skin', srgb: '#c29682' },
  { name: 'Blue Sky', srgb: '#627a9d' },
  { name: 'Foliage', srgb: '#576c43' },
  { name: 'Blue Flower', srgb: '#8580b1' },
  { name: 'Bluish Green', srgb: '#67bdaa' },
  // 第二行 - 原色
  { name: 'Orange', srgb: '#d67e2c' },
  { name: 'Purplish Blue', srgb: '#505ba6' },
  { name: 'Moderate Red', srgb: '#c15a63' },
  { name: 'Purple', srgb: '#5e3c6c' },
  { name: 'Yellow Green', srgb: '#9dbc40' },
  { name: 'Orange Yellow', srgb: '#e0a32e' },
  // 第三行
  { name: 'Blue', srgb: '#383d96' },
  { name: 'Green', srgb: '#469449' },
  { name: 'Red', srgb: '#af363c' },
  { name: 'Yellow', srgb: '#e7c71f' },
  { name: 'Magenta', srgb: '#bb5695' },
  { name: 'Cyan', srgb: '#0885a1' },
  // 第四行 - 灰阶
  { name: 'White', srgb: '#f3f3f2' },
  { name: 'Neutral 8', srgb: '#c8c8c8' },
  { name: 'Neutral 6.5', srgb: '#a0a0a0' },
  { name: 'Neutral 5', srgb: '#7a7a79' },
  { name: 'Neutral 3.5', srgb: '#555555' },
  { name: 'Black', srgb: '#343434' },
];

export function ColorAccuracyTest() {
  const [showLabels, setShowLabels] = useState(true);

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>色彩准确度测试 - ColorChecker 24色卡</CardTitle>
          <CardDescription>
            对比标准色卡评估屏幕色彩还原能力
          </CardDescription>
        </CardHeader>
        <CardContent>
          <div className="grid grid-cols-6 gap-1">
            {COLOR_CHECKER_COLORS.map((color, index) => (
              <div
                key={index}
                className="aspect-square rounded flex items-end justify-center pb-1"
                style={{ backgroundColor: color.srgb }}
              >
                {showLabels && (
                  <span className="text-xs text-white drop-shadow-lg">
                    {color.name}
                  </span>
                )}
              </div>
            ))}
          </div>

          <div className="flex items-center gap-2 mt-4">
            <Switch
              checked={showLabels}
              onCheckedChange={setShowLabels}
            />
            <Label>显示色块名称</Label>
          </div>
        </CardContent>
      </Card>

      {/* 灰阶测试 */}
      <GrayscaleTest />
      
      {/* 渐变测试 */}
      <GradientTest />
    </div>
  );
}

function GrayscaleTest() {
  const steps = 16; // 灰阶级数
  
  return (
    <Card>
      <CardHeader>
        <CardTitle>灰阶测试</CardTitle>
        <CardDescription>检查从纯黑到纯白的过渡是否平滑</CardDescription>
      </CardHeader>
      <CardContent>
        <div className="flex h-16">
          {Array.from({ length: steps }, (_, i) => {
            const value = Math.round((i / (steps - 1)) * 255);
            return (
              <div
                key={i}
                className="flex-1 h-full"
                style={{ backgroundColor: `rgb(${value}, ${value}, ${value})` }}
              />
            );
          })}
        </div>
        <p className="text-sm text-muted-foreground mt-2">
          优秀的屏幕应该能够区分所有灰阶级别，没有明显的断层或色偏
        </p>
      </CardContent>
    </Card>
  );
}

function GradientTest() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>色彩渐变测试</CardTitle>
        <CardDescription>检查色彩过渡是否平滑，是否存在色带</CardDescription>
      </CardHeader>
      <CardContent className="space-y-3">
        <div
          className="h-12 rounded"
          style={{ background: 'linear-gradient(to right, #000000, #FF0000)' }}
        />
        <div
          className="h-12 rounded"
          style={{ background: 'linear-gradient(to right, #000000, #00FF00)' }}
        />
        <div
          className="h-12 rounded"
          style={{ background: 'linear-gradient(to right, #000000, #0000FF)' }}
        />
        <div
          className="h-12 rounded"
          style={{ background: 'linear-gradient(to right, #FF0000, #00FF00, #0000FF)' }}
        />
      </CardContent>
    </Card>
  );
}
```

---

### 模块5: 响应时间测试（拖影测试）

**功能描述**: 通过移动物体观察拖影程度，评估屏幕响应时间

**功能点**:
- 多速度移动方块
- UFO测试图案
- 不同背景对比度
- 响应时间等级评估

**技术实现**:

```typescript
interface SpeedConfig {
  name: string;
  pixelsPerFrame: number;
}

const SPEEDS: SpeedConfig[] = [
  { name: '慢速', pixelsPerFrame: 2 },
  { name: '中速', pixelsPerFrame: 5 },
  { name: '快速', pixelsPerFrame: 10 },
  { name: '极速', pixelsPerFrame: 20 },
];

export function ResponseTimeTest() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const [speed, setSpeed] = useState(SPEEDS[1]);
  const [showUFO, setShowUFO] = useState(true);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;

    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    let x = 0;
    const height = canvas.height;
    const objectHeight = 40;
    const objectWidth = showUFO ? 80 : 40;

    const animate = () => {
      // 清除画布
      ctx.fillStyle = '#1a1a1a';
      ctx.fillRect(0, 0, canvas.width, canvas.height);

      // 绘制参考线
      ctx.strokeStyle = '#333';
      ctx.lineWidth = 1;
      for (let i = 0; i < canvas.width; i += 50) {
        ctx.beginPath();
        ctx.moveTo(i, 0);
        ctx.lineTo(i, height);
        ctx.stroke();
      }

      // 绘制移动物体
      const y = (height - objectHeight) / 2;
      
      if (showUFO) {
        // UFO图案
        ctx.fillStyle = '#22c55e';
        ctx.beginPath();
        ctx.ellipse(x + objectWidth / 2, y + objectHeight / 2, objectWidth / 2, objectHeight / 3, 0, 0, Math.PI * 2);
        ctx.fill();
        
        ctx.fillStyle = '#4ade80';
        ctx.beginPath();
        ctx.ellipse(x + objectWidth / 2, y + objectHeight / 3, objectWidth / 4, objectHeight / 5, 0, 0, Math.PI * 2);
        ctx.fill();
      } else {
        // 简单方块
        ctx.fillStyle = '#22c55e';
        ctx.fillRect(x, y, objectWidth, objectHeight);
      }

      x += speed.pixelsPerFrame;
      if (x > canvas.width) x = -objectWidth;

      requestAnimationFrame(animate);
    };

    const animationId = requestAnimationFrame(animate);
    return () => cancelAnimationFrame(animationId);
  }, [speed, showUFO]);

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>响应时间测试（拖影测试）</CardTitle>
          <CardDescription>
            观察移动物体的拖影程度，评估屏幕响应时间
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-4">
          <canvas
            ref={canvasRef}
            width={800}
            height={150}
            className="w-full h-36 rounded-lg"
          />

          <div className="flex flex-wrap gap-2">
            {SPEEDS.map(s => (
              <Button
                key={s.name}
                variant={speed.name === s.name ? 'default' : 'outline'}
                onClick={() => setSpeed(s)}
              >
                {s.name}
              </Button>
            ))}
          </div>

          <div className="flex items-center gap-2">
            <Switch checked={showUFO} onCheckedChange={setShowUFO} />
            <Label>UFO图案模式</Label>
          </div>

          <Alert>
            <AlertDescription>
              💡 拖影越少说明响应时间越快。优秀的屏幕（1ms响应）几乎看不到拖影，
              普通屏幕（5ms+）会有明显的残影拖尾。
            </AlertDescription>
          </Alert>
        </CardContent>
      </Card>
    </div>
  );
}
```

---

### 模块6: 亮度均匀性测试

**功能描述**: 检测屏幕各区域亮度是否均匀

**功能点**:
- 全屏灰色背景
- 九宫格区域划分
- 明暗对比指示
- IPS/VA面板漏光检测

**技术实现**:

```typescript
export function UniformityTest() {
  const [brightness, setBrightness] = useState(50);
  const [showGrid, setShowGrid] = useState(true);

  const grayValue = Math.round((brightness / 100) * 255);
  const bgColor = `rgb(${grayValue}, ${grayValue}, ${grayValue})`;

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>亮度均匀性测试</CardTitle>
          <CardDescription>
            在暗室中观察屏幕各区域亮度是否一致
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-4">
          <div
            className="w-full h-64 rounded-lg relative"
            style={{ backgroundColor: bgColor }}
          >
            {showGrid && (
              <div className="absolute inset-0 grid grid-cols-3 grid-rows-3">
                {Array.from({ length: 9 }, (_, i) => (
                  <div
                    key={i}
                    className="border border-white/20 flex items-center justify-center"
                  >
                    <span className="text-white/50 text-sm">
                      区域 {i + 1}
                    </span>
                  </div>
                ))}
              </div>
            )}
          </div>

          <div className="space-y-2">
            <Label>亮度级别: {brightness}%</Label>
            <Slider
              value={[brightness]}
              onValueChange={([v]) => setBrightness(v)}
              min={0}
              max={100}
              step={1}
            />
          </div>

          <div className="flex items-center gap-2">
            <Switch checked={showGrid} onCheckedChange={setShowGrid} />
            <Label>显示九宫格</Label>
          </div>

          <Alert>
            <AlertDescription>
              💡 在5%左右的低亮度下，如果四角出现明显亮光，说明存在漏光问题（IPS屏幕常见）。
              调至50%灰度观察是否有云状不均匀（阴阳屏）。
            </AlertDescription>
          </Alert>
        </CardContent>
      </Card>
    </div>
  );
}
```

---

### 模块7: 色域覆盖测试

**功能描述**: 显示不同色域的代表色块，评估屏幕色域覆盖

**功能点**:
- sRGB色域测试色
- DCI-P3色域测试色
- Rec.2020色域测试色（参考）
- 色域外颜色标识

**技术实现**:

```typescript
interface ColorGamutConfig {
  name: string;
  colors: { name: string; value: string }[];
  description: string;
}

const COLOR_GAMUTS: ColorGamutConfig[] = [
  {
    name: 'sRGB',
    description: '标准色域，大多数屏幕支持',
    colors: [
      { name: 'sRGB Red', value: '#FF0000' },
      { name: 'sRGB Green', value: '#00FF00' },
      { name: 'sRGB Blue', value: '#0000FF' },
    ],
  },
  {
    name: 'DCI-P3',
    description: '广色域，iPhone/Mac标准',
    colors: [
      { name: 'P3 Red', value: 'color(display-p3 1 0 0)' },
      { name: 'P3 Green', value: 'color(display-p3 0 1 0)' },
      { name: 'P3 Blue', value: 'color(display-p3 0 0 1)' },
    ],
  },
];

export function ColorGamutTest() {
  const [activeGamut, setActiveGamut] = useState('sRGB');

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>色域覆盖测试</CardTitle>
          <CardDescription>
            比较不同色域标准下的颜色显示能力
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-4">
          <Tabs value={activeGamut} onValueChange={setActiveGamut}>
            <TabsList>
              {COLOR_GAMUTS.map(gamut => (
                <TabsTrigger key={gamut.name} value={gamut.name}>
                  {gamut.name}
                </TabsTrigger>
              ))}
            </TabsList>

            {COLOR_GAMUTS.map(gamut => (
              <TabsContent key={gamut.name} value={gamut.name}>
                <p className="text-sm text-muted-foreground mb-4">
                  {gamut.description}
                </p>
                <div className="grid grid-cols-3 gap-4">
                  {gamut.colors.map(color => (
                    <div
                      key={color.name}
                      className="aspect-video rounded-lg flex items-end justify-center pb-2"
                      style={{ backgroundColor: color.value }}
                    >
                      <span className="text-white text-sm drop-shadow-lg">
                        {color.name}
                      </span>
                    </div>
                  ))}
                </div>
              </TabsContent>
            ))}
          </Tabs>

          <Alert>
            <AlertDescription>
              💡 如果P3色域的红色和sRGB红色看起来完全一样，说明您的屏幕不支持广色域。
              支持广色域的屏幕会显示更加鲜艳饱和的颜色。
            </AlertDescription>
          </Alert>
        </CardContent>
      </Card>
    </div>
  );
}
```

---

### 模块8: HDR显示效果测试

**功能描述**: 测试屏幕HDR显示能力

**功能点**:
- HDR高光测试图
- 对比度测试
- 高动态范围渐变
- SDR/HDR对比

**技术实现**:

```typescript
export function HDRTest() {
  const [hdrEnabled, setHdrEnabled] = useState(false);

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>HDR显示效果测试</CardTitle>
          <CardDescription>
            测试屏幕的高动态范围显示能力
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-4">
          {/* 对比度测试 */}
          <div className="grid grid-cols-2 gap-4">
            <div className="bg-black p-4 rounded-lg">
              <div className="text-white text-center">
                纯黑区域
                <div className="text-xs text-gray-400 mt-1">
                  应该完全黑暗（OLED）或接近黑暗（LCD）
                </div>
              </div>
            </div>
            <div className="bg-white p-4 rounded-lg">
              <div className="text-black text-center">
                纯白区域
                <div className="text-xs text-gray-600 mt-1">
                  应该足够明亮且不刺眼
                </div>
              </div>
            </div>
          </div>

          {/* 高光细节测试 */}
          <div className="space-y-2">
            <Label>高光细节测试</Label>
            <div
              className="h-24 rounded-lg"
              style={{
                background: 'linear-gradient(to right, #FFFFFF, #FFFFCC, #FFFF99, #FFFF66, #FFFF33, #FFFF00)',
              }}
            />
            <p className="text-xs text-muted-foreground">
              应该能够区分从纯白到亮黄的所有层次
            </p>
          </div>

          {/* 暗部细节测试 */}
          <div className="space-y-2">
            <Label>暗部细节测试</Label>
            <div
              className="h-24 rounded-lg"
              style={{
                background: 'linear-gradient(to right, #000000, #0A0A0A, #141414, #1E1E1E, #282828, #323232)',
              }}
            />
            <p className="text-xs text-muted-foreground">
              应该能够区分从纯黑到深灰的所有层次
            </p>
          </div>

          <Alert>
            <AlertDescription>
              💡 HDR屏幕应该能同时保留高光和暗部的细节，
              普通SDR屏幕在极端亮度下会丢失细节。
            </AlertDescription>
          </Alert>
        </CardContent>
      </Card>
    </div>
  );
}
```

---

### 模块9: OLED烧屏风险测试

**功能描述**: 检测OLED屏幕是否存在烧屏痕迹

**功能点**:
- 均匀灰色背景检测
- 状态栏区域重点检测
- 导航栏区域检测
- 常用APP图标区域检测

**技术实现**:

```typescript
export function BurnInTest() {
  const [testColor, setTestColor] = useState('#808080');
  const [isFullscreen, setIsFullscreen] = useState(false);
  const containerRef = useRef<HTMLDivElement>(null);

  const testColors = [
    { name: '灰色', value: '#808080' },
    { name: '红色', value: '#FF0000' },
    { name: '绿色', value: '#00FF00' },
    { name: '蓝色', value: '#0000FF' },
  ];

  const enterFullscreen = async () => {
    if (containerRef.current) {
      await containerRef.current.requestFullscreen();
      setIsFullscreen(true);
    }
  };

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>OLED烧屏检测</CardTitle>
          <CardDescription>
            检测OLED屏幕是否存在老化/烧屏痕迹
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-4">
          <div
            ref={containerRef}
            className="w-full h-64 rounded-lg flex items-center justify-center"
            style={{ backgroundColor: testColor }}
            onClick={() => isFullscreen && document.exitFullscreen()}
          >
            {!isFullscreen && (
              <Button variant="secondary" onClick={enterFullscreen}>
                全屏检测
              </Button>
            )}
          </div>

          <div className="flex flex-wrap gap-2">
            {testColors.map(color => (
              <Button
                key={color.value}
                variant={testColor === color.value ? 'default' : 'outline'}
                onClick={() => setTestColor(color.value)}
              >
                {color.name}
              </Button>
            ))}
          </div>

          <Alert variant="destructive">
            <AlertDescription>
              ⚠️ 检测要点：
              <ul className="list-disc list-inside mt-2 space-y-1">
                <li>状态栏区域（电量、时间图标位置）</li>
                <li>底部导航栏区域</li>
                <li>键盘常用区域</li>
                <li>社交App底部图标位置</li>
              </ul>
              如果在灰色背景下看到淡淡的图标轮廓，说明存在烧屏。
            </AlertDescription>
          </Alert>
        </CardContent>
      </Card>
    </div>
  );
}
```

---

### 模块10: 可视角度测试

**功能描述**: 利用设备陀螺仪检测用户观看角度，测试不同角度下的显示效果

**功能点**:
- 陀螺仪角度检测
- 实时显示观看角度
- 不同角度色彩提示
- IPS/VA/TN面板特性说明

**技术实现**:

```typescript
export function ViewingAngleTest() {
  const [angle, setAngle] = useState({ alpha: 0, beta: 0, gamma: 0 });
  const [isSupported, setIsSupported] = useState(true);

  useEffect(() => {
    if (!('DeviceOrientationEvent' in window)) {
      setIsSupported(false);
      return;
    }

    const handleOrientation = (event: DeviceOrientationEvent) => {
      setAngle({
        alpha: event.alpha || 0,
        beta: event.beta || 0,
        gamma: event.gamma || 0,
      });
    };

    // iOS 13+ 需要请求权限
    if (typeof (DeviceOrientationEvent as any).requestPermission === 'function') {
      (DeviceOrientationEvent as any).requestPermission()
        .then((permission: string) => {
          if (permission === 'granted') {
            window.addEventListener('deviceorientation', handleOrientation);
          }
        });
    } else {
      window.addEventListener('deviceorientation', handleOrientation);
    }

    return () => {
      window.removeEventListener('deviceorientation', handleOrientation);
    };
  }, []);

  const getAngleQuality = (gamma: number) => {
    const absGamma = Math.abs(gamma);
    if (absGamma < 15) return { text: '正视角度', color: 'text-green-500' };
    if (absGamma < 30) return { text: '轻微偏转', color: 'text-yellow-500' };
    if (absGamma < 45) return { text: '中度偏转', color: 'text-orange-500' };
    return { text: '大角度偏转', color: 'text-red-500' };
  };

  const quality = getAngleQuality(angle.gamma);

  return (
    <div className="p-6 space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>可视角度测试</CardTitle>
          <CardDescription>
            倾斜设备观察不同角度下的显示效果
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-4">
          {!isSupported ? (
            <Alert variant="destructive">
              <AlertDescription>
                您的设备不支持陀螺仪，无法进行可视角度测试
              </AlertDescription>
            </Alert>
          ) : (
            <>
              <div className="grid grid-cols-3 gap-4 text-center">
                <div>
                  <div className="text-2xl font-bold">{angle.beta.toFixed(1)}°</div>
                  <div className="text-sm text-muted-foreground">前后倾斜</div>
                </div>
                <div>
                  <div className={`text-2xl font-bold ${quality.color}`}>
                    {Math.abs(angle.gamma).toFixed(1)}°
                  </div>
                  <div className="text-sm text-muted-foreground">左右倾斜</div>
                </div>
                <div>
                  <div className="text-2xl font-bold">{angle.alpha.toFixed(1)}°</div>
                  <div className="text-sm text-muted-foreground">旋转角度</div>
                </div>
              </div>

              <div className={`text-center text-lg font-medium ${quality.color}`}>
                {quality.text}
              </div>

              {/* 色彩测试区域 */}
              <div className="grid grid-cols-3 gap-2">
                <div className="aspect-square bg-red-500 rounded" />
                <div className="aspect-square bg-green-500 rounded" />
                <div className="aspect-square bg-blue-500 rounded" />
              </div>

              <Alert>
                <AlertDescription>
                  💡 IPS屏幕可视角度约178°，色彩几乎不变。
                  VA屏幕侧看会明显变色。TN屏幕可视角度最差。
                </AlertDescription>
              </Alert>
            </>
          )}
        </CardContent>
      </Card>
    </div>
  );
}
```

---

## 📁 项目目录结构

```text
apps/web/app/screen-test/
├── page.tsx                      # 主页面（测试导航）
├── layout.tsx                    # 布局组件
├── components/
│   ├── TestCard.tsx              # 测试项目卡片
│   ├── FullscreenContainer.tsx   # 全屏容器组件
│   └── TestProgress.tsx          # 测试进度组件
├── dead-pixel/
│   └── page.tsx                  # 坏点检测页面
├── refresh-rate/
│   └── page.tsx                  # 刷新率测试页面
├── pwm-flicker/
│   └── page.tsx                  # PWM频闪检测页面
├── color-accuracy/
│   └── page.tsx                  # 色彩准确度测试页面
├── response-time/
│   └── page.tsx                  # 响应时间测试页面
├── uniformity/
│   └── page.tsx                  # 亮度均匀性测试页面
├── color-gamut/
│   └── page.tsx                  # 色域覆盖测试页面
├── hdr/
│   └── page.tsx                  # HDR测试页面
├── burn-in/
│   └── page.tsx                  # OLED烧屏检测页面
├── viewing-angle/
│   └── page.tsx                  # 可视角度测试页面
├── hooks/
│   ├── useFullscreen.ts          # 全屏Hook
│   ├── useFrameRate.ts           # 帧率检测Hook
│   └── useDeviceOrientation.ts   # 陀螺仪Hook
├── lib/
│   ├── colors.ts                 # 颜色常量定义
│   └── utils.ts                  # 工具函数
└── types/
    └── index.ts                  # 类型定义
```

---

## 📋 开发任务清单

### 1️⃣ 项目初始化

- [ ] 在apps/web/app下创建screen-test目录
- [ ] 配置页面路由结构
- [ ] 创建共享组件和Hooks
- [ ] 配置PWA支持（离线使用）

### 2️⃣ 坏点检测模块

- [ ] 实现纯色背景切换
- [ ] 实现全屏API调用
- [ ] 实现键盘/触摸事件监听
- [ ] 添加使用说明引导

### 3️⃣ 刷新率测试模块 ⭐核心

- [ ] 实现requestAnimationFrame帧率测量
- [ ] 实现帧时间统计
- [ ] 实现可视化动画指示器
- [ ] 添加刷新率等级判定

### 4️⃣ PWM频闪检测模块

- [ ] 实现多频率闪烁图案
- [ ] 实现频率选择器
- [ ] 添加相机拍摄指导说明

### 5️⃣ 色彩准确度测试模块

- [ ] 实现24色ColorChecker色卡
- [ ] 实现灰阶测试
- [ ] 实现渐变测试
- [ ] 添加色彩说明

### 6️⃣ 响应时间测试模块

- [ ] 实现Canvas动画
- [ ] 实现多速度选择
- [ ] 实现UFO测试图案

### 7️⃣ 其他测试模块

- [ ] 亮度均匀性测试
- [ ] 色域覆盖测试
- [ ] HDR显示测试
- [ ] OLED烧屏检测
- [ ] 可视角度测试（陀螺仪）

### 8️⃣ UI/UX优化

- [ ] 响应式布局适配
- [ ] 深色/浅色主题
- [ ] 测试结果汇总页
- [ ] 分享功能

### 9️⃣ 测试与发布

- [ ] 多设备测试（手机/平板/电脑）
- [ ] 浏览器兼容性测试
- [ ] PWA功能测试
- [ ] 部署上线

---

## 📱 UI/UX设计规范

### 色彩方案（参考shadcn/ui暗色主题）

| 用途 | 颜色 | 说明 |
|------|------|------|
| Primary | #22c55e | 主色调-绿色 |
| Background | #09090b | 深色背景 |
| Card | #18181b | 卡片背景 |
| Border | #27272a | 边框颜色 |
| Text Primary | #fafafa | 主要文字 |
| Text Muted | #a1a1aa | 次要文字 |
| Success | #22c55e | 成功/通过 |
| Warning | #eab308 | 警告 |
| Error | #ef4444 | 错误/不通过 |

### 布局规范

- **移动优先**: 所有测试需要在手机上有良好体验
- **全屏模式**: 坏点检测等测试需要支持全屏
- **手势操作**: 支持滑动切换、点击确认
- **大按钮**: 触摸友好的交互元素

### 导航结构

```
首页（测试列表）
├── 坏点检测
├── 刷新率测试
├── PWM频闪检测
├── 色彩准确度
├── 响应时间
├── 亮度均匀性
├── 色域覆盖
├── HDR测试
├── OLED烧屏检测
└── 可视角度测试
```

---

## ⚠️ 技术风险与解决方案

### 风险1: 浏览器API限制

**问题**: 部分浏览器限制全屏API或陀螺仪API

**解决方案**:
1. 检测API支持情况，提供降级方案
2. 引导用户使用Chrome/Safari
3. 对不支持的功能显示提示

### 风险2: 刷新率测量精度

**问题**: requestAnimationFrame受浏览器节流影响

**解决方案**:
1. 使用足够大的采样窗口
2. 排除异常值
3. 提示用户关闭省电模式

### 风险3: 色彩显示受限

**问题**: 不同设备色彩管理不同，P3色域可能显示异常

**解决方案**:
1. 使用CSS color()函数
2. 检测浏览器色彩配置文件支持
3. 提供sRGB回退方案

### 风险4: PWM检测局限性

**问题**: Web无法真正测量PWM频率，只能模拟

**解决方案**:
1. 提供清晰的使用指导
2. 建议用户使用专业相机APP
3. 说明测试原理和局限性

---

## 📊 验收标准

### 功能验收

- [ ] 所有10个测试模块可正常运行
- [ ] 全屏模式正常进入/退出
- [ ] 刷新率检测结果准确
- [ ] 色彩显示正确
- [ ] 动画流畅无卡顿
- [ ] 陀螺仪数据正确读取

### 兼容性验收

- [ ] Chrome 90+ ✓
- [ ] Safari 15+ ✓
- [ ] Firefox 90+ ✓
- [ ] Edge 90+ ✓
- [ ] iOS Safari ✓
- [ ] Android Chrome ✓

### 性能验收

- [ ] 首屏加载 < 2s
- [ ] 动画帧率 >= 屏幕刷新率
- [ ] 内存占用 < 100MB
- [ ] PWA离线可用

---

## 📚 参考资料

1. [Canvas API - MDN](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API)
2. [requestAnimationFrame - MDN](https://developer.mozilla.org/en-US/docs/Web/API/window/requestAnimationFrame)
3. [Fullscreen API - MDN](https://developer.mozilla.org/en-US/docs/Web/API/Fullscreen_API)
4. [Device Orientation API - MDN](https://developer.mozilla.org/en-US/docs/Web/API/Device_orientation_events)
5. [WebGL Fundamentals](https://webglfundamentals.org/)
6. [UFO Test](https://www.testufo.com/) - 参考网站
7. [Display Test Patterns](https://www.eizo.com/library/basics/test_patterns/) - 测试图案参考

---

## 📝 更新日志

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2025-12-27 | v0.1 | 初始文档创建 |

---

**文档维护者**: 开发团队  
**最后更新**: 2025年12月27日
